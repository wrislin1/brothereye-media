# Session summary — <project> — <YYYY-MM-DD HH:MM>

## TL;DR
<1-2 sentences capturing what we set out to do and whether we got there>

## Tasks completed
1. <task — one line>
2. ...

## Files changed
<group by area; one line per file>
- `path/to/file` — <one-line description of the change>
- ...

## Key commands run
<the ones that actually mattered — tests, builds, docker ops, ssh — not every grep>
- `<command>` — <what it accomplished>
- ...

## Decisions & rationale
- **Decision:** <what we chose> — **Why:** <reason; ideally cites a constraint, preference, or past incident>
- ...

## Verification
- ✅ <thing that was confirmed working>
- ⚠️ <thing that was partially verified or has a known caveat>
- ❌ <thing that failed or remains untested>

## Deferred / open items
- <thing we explicitly skipped or punted, with one line of context>
- ...

## Follow-up
- (optional) Run `/skill-evolve` if any of the surprises above should become forever rules.
- (optional) <pointer to another skill or task that should pick up next>
